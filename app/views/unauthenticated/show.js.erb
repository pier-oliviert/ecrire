(function() {
  var $admin = $("#new_session")
  if ($admin.length > 0) {
    $html.find("#session_email").focus()
    return
  }
  var $html = $("<%= j render("menu") %>")
  $("body").prepend($html)
  $html.find("#session_email").focus()
})()
