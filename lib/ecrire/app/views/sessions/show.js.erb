var $admin = $("#new_session")
if ($admin.length > 0) {
  $admin.find("#session_email").focus()
  return
}
var $html = $("<%= j content_tag(:nav, render("form"), class: %w(session wrapper)) %>")
$("body").prepend($html)
$html.find("#session_email").focus()
