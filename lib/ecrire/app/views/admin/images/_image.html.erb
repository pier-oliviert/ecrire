<%= content_tag :li, class: %w(image), id: "image-#{image.id}" do %>
  <sidebar class="options">

  <%= content_tag :a, '&#9733;'.html_safe, href: admin_image_path(record), remote: true, class: %w(favorite action image), 'data-token' => form_authenticity_token, 'data-favorite' => !record.favorite? do |a| %>
    <% a.css << 'active' if image.favorite? %>
  <% end %>

  <%= content_tag :a, '&#59177;'.html_safe, href: admin_image_path(record), class: %w(destroy action image), 'data-token' => form_authenticity_token %>
  </sidebar>

  <%= content_tag :div, draggable: true do |div| %>

    <% div['style'] = "background-image: url(#{record.url});" %>
    <% div['data-url'] = image.url %>
    <% div['data-alt'] = image.key %>
    <% div['ondragstart'] = "window.Editor.imageImporter.configureEvent(event)" %>

  <% end %>
<% end %>
