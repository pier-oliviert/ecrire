$textarea = $('textarea.content')
$article = $('article.content')
$image = $('<%= j image_tag(@image.url) %>')
df = document.createDocumentFragment()
body = document.createElement('body')
body.innerHTML = $textarea.val()

$form = $('form.image.replaceable')
idx = $article.find('form.image').index($form)

$(body).find('img').filter(':not([src])').eq(idx).replaceWith($image)

$textarea.val(body.innerHTML)

$form.replaceWith($image)
$('form.post.editor.autosave').data('outdated', true)
