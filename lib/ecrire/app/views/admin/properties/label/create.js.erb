$placeholder = $('#label-<%= @property.id %>')
$button = $('<%= j destroy_label_button(@property, post) %>')
if ($placeholder.length > 0) {
  $placeholder.replaceWith($button)
}
else {
  $('form.labels.new').siblings('.existing').append($button)
}
